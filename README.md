# PR assets

Images referenced from the description of the `feat/tray-indicator` pull request.
This branch is not part of the proposed change and is never merged.

- `tray-states.png` — the four tray states, built from the ARGB32 pixmaps read back
  off each running tray's `StatusNotifierItem` interface.
- `tray-menu.png` — the menu, on KDE Plasma 6.
